# Critical Patterns (Required Reading)

Graduated patterns from `docs/solutions/`. Every agent MUST read this file before writing code.

These patterns represent non-obvious mistakes that have been made before. Each was promoted here because the solution wasn't immediately obvious and the mistake is likely to recur.

---

_No patterns graduated yet. Patterns are promoted here via `/workflows-compound` when a learning meets graduation criteria._
